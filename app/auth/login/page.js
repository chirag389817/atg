import React from "react";
import LoginForm from "./LoginForm";

export const metadata = {
    title: "Login - ATG",
    description: "Login to ATG"
};

function page() {
    return <LoginForm />;
}

export default page;
